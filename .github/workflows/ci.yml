name: CI

on:
  pull_request:
    branches: [ main, release-0.1.z, v1/thm-a-governance-contract-posture-recovery, feat/v1-overhaul ]
  push:
    branches: [ main, release-0.1.z, v1/thm-a-governance-contract-posture-recovery, feat/v1-overhaul ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # CHANGE THIS BLOCK TO MATCH YOUR STACK
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Bootstrap virtual environment
        run: |
          python -m venv .venv

      - name: Install dependencies
        run: |
          ./.venv/bin/python -m pip install --upgrade pip
          ./.venv/bin/python -m pip install -r requirements.txt pytest

      - name: Run tests
        run: |
          PYTHONPATH=src ./.venv/bin/python -m pytest

  security:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Bootstrap virtual environment
        run: |
          python -m venv .venv

      - name: Install dependencies
        run: |
          ./.venv/bin/python -m pip install --upgrade pip
          ./.venv/bin/python -m pip install -r requirements.txt bandit pip-audit

      - name: Run bandit
        run: |
          ./.venv/bin/python -m bandit -r src

      - name: Run pip-audit
        run: |
          ./.venv/bin/python -m pip_audit

  quality:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Bootstrap virtual environment
        run: |
          python -m venv .venv

      - name: Install dependencies
        run: |
          ./.venv/bin/python -m pip install --upgrade pip
          ./.venv/bin/python -m pip install -r requirements.txt mypy ruff

      - name: Run source format policy
        run: |
          ./.venv/bin/python project/scripts/format_policy.py --target src --mode block

      - name: Run tests format policy
        run: |
          ./.venv/bin/python project/scripts/format_policy.py --target tests --mode warn

      - name: Run ruff lint
        run: |
          ./.venv/bin/python -m ruff check .

      - name: Run mypy
        run: |
          ./.venv/bin/python -m mypy src

  governance:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Bootstrap virtual environment
        run: |
          python -m venv .venv

      - name: Install dependencies
        run: |
          ./.venv/bin/python -m pip install --upgrade pip
          ./.venv/bin/python -m pip install -r requirements.txt pytest

      - name: Run governance checks
        run: |
          ./.venv/bin/python project/scripts/verify_governance.py --check all
