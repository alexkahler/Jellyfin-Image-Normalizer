# WI-001

## 1. Objective
Add architecture guard tests as a governance extension for Track 1 structure-gate bullets 2 and 3: non-entry exit ratchet enforcement and conditional import-boundary checks.

## 2. In-scope / Out-of-scope
In-scope: `project/scripts/architecture_contract.py`, `project/scripts/architecture_checks.py`, `project/architecture-baseline.json`, governance selector wiring for `--check architecture`, unit tests, and docs updates.
Out-of-scope: runtime refactors in `src/jfin`, full elimination of existing non-entry exits, structure-gate bullets 1/4/5/6, and Track 2 CLI/config redesign.

## 3. Public interfaces affected
- `./.venv/bin/python project/scripts/verify_governance.py --check architecture`
- `./.venv/bin/python project/scripts/verify_governance.py --check architecture --print-baseline`
- `./.venv/bin/python project/scripts/verify_governance.py --check all` (includes architecture checks)
- `project/architecture-baseline.json`

## 4. Acceptance criteria
- New non-entry exit usages fail unless pre-approved in `project/architecture-baseline.json`.
- Baseline counter growth fails; equal or reduced counts pass (reductions emit warnings).
- Domain boundary checks fail on forbidden imports under `src/jfin/domain/` when that path exists.
- App/services boundary checks fail only on adapter imports under `src/jfin/app/services/` when that path exists.
- Governance CI entrypoint remains `./.venv/bin/python project/scripts/verify_governance.py --check all`.

## 5. Verification commands (<10 min)
- `PYTHONPATH=src ./.venv/bin/python -m pytest -q tests/test_architecture_checks.py`
- `PYTHONPATH=src ./.venv/bin/python -m pytest -q tests/test_governance_checks.py -k architecture`
- `./.venv/bin/python project/scripts/verify_governance.py --check architecture`
- `./.venv/bin/python project/scripts/verify_governance.py --check architecture --print-baseline`
- `rg -n "verify_governance.py --check all" .github/workflows/ci.yml project/scripts/governance_checks.py`
- `./.venv/bin/python project/scripts/verify_governance.py --check all`

## 6. Rollback step
Revert WI-001 architecture commits in reverse order with `git revert`: docs/WI updates, governance wiring, architecture checks, then architecture baseline artifact/contract.

## 7. Behavior-preservation statement
Preserved. WI-001 Slice 5 adds governance-only architecture checks and docs; no runtime behavior changes are made in `src/jfin`.
