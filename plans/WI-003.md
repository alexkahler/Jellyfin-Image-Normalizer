# WI-003

## 1. Objective
Add an imaging characterization + golden harness for `IMG-*` parity behavior IDs with governance validation for baseline/manifest linkage and artifact budgets.

## 2. In-scope / Out-of-scope
In-scope: imaging characterization baselines, golden manifest + expected artifacts, hybrid corpus (synthetic + self-made real-ish fixtures), parity IMG row linkage updates, characterization governance extension, and docs updates.
Out-of-scope: runtime behavior changes in `src/jfin`, pipeline/client behavior ownership, route flips to `v1`, and parity status changes from `preserved`.

## 3. Public interfaces affected
- `tests/characterization/baselines/imaging_contract_baseline.json`
- `tests/golden/imaging/manifest.json`
- `python project/scripts/verify_governance.py --check characterization`
- `project/parity-matrix.md` (`IMG-*` row linkage to baseline anchors and characterization owner tests)

## 4. Acceptance criteria
- All 5 `IMG-*` behavior IDs are represented in `imaging_contract_baseline.json` with required transform properties and `golden_key` linkage.
- Golden manifest includes required metadata (`python_version`, `pillow_version`, `generated_at`) and resolvable expected artifact paths.
- Characterization governance check validates IMG baseline schema, parity linkage, manifest linkage, and both artifact budgets.
- No imaging characterization test imports `jfin.pipeline` or `jfin.client`.
- `python project/scripts/verify_governance.py --check all` is run and recorded; no new failures beyond pre-existing LOC failures.

## 5. Verification commands (<10 min)
- `PYTHONPATH=src python -m pytest -q tests/characterization/imaging_contract`
- `PYTHONPATH=src python -m pytest -q tests/test_characterization_checks.py tests/test_characterization_checks_links.py tests/test_characterization_checks_imaging.py`
- `PYTHONPATH=src python -m pytest -q tests/characterization -k harness`
- `python project/scripts/verify_governance.py --check characterization`
- `python project/scripts/verify_governance.py --check parity`
- `python project/scripts/verify_governance.py --check all`

## 6. Rollback step
Revert WI-003 commits in reverse order with `git revert`: docs/parity updates, governance checker wiring/tests, then imaging characterization/golden artifacts.

## 7. Behavior-preservation statement
Preserved. WI-003 adds imaging characterization, governance, and documentation only; no runtime behavior changes in `src/jfin`.
