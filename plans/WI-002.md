# WI-002

## 1. Objective
Add a parity matrix skeleton with behavior inventory and an explicit route-fence artifact, validated through governance checks.

## 2. In-scope / Out-of-scope
In-scope: `project/parity-matrix.md`, `project/route-fence.md`, parity validators, governance wiring, and tests.
Out-of-scope: runtime behavior changes in `src/jfin`, route flips to `v1`, and characterization/golden harness implementation.

## 3. Public interfaces affected
- `project/parity-matrix.md`
- `project/route-fence.md`
- `./.venv/bin/python project/scripts/verify_governance.py --check parity`

## 4. Acceptance criteria
- Parity matrix includes required schema columns and seeded behavior IDs.
- Route fence includes required command/mode rows and valid `v0|v1` route values.
- `--check parity` passes and `--check all` includes parity validation.
- New parity tests pass.

## 5. Verification commands (<10 min)
- `rg -n "behavior_id|baseline_source|current_result|status|owner_test|approval_ref" project/parity-matrix.md`
- `rg -n "command|route\\(v0\\|v1\\)|owner slice|parity status|config_validate" project/route-fence.md`
- `./.venv/bin/python project/scripts/verify_governance.py --check parity`
- `PYTHONPATH=src ./.venv/bin/python -m pytest -q tests/test_parity_checks.py`
- `PYTHONPATH=src ./.venv/bin/python -m pytest -q tests/test_governance_checks.py`

## 6. Rollback step
Revert WI-002 commits in reverse order using `git revert`: tests/docs, checker wiring, then artifacts.

## 7. Behavior-preservation statement
Preserved. This slice adds governance and parity metadata only; no runtime behavior changes in `src/jfin`.
