# WI-004

## 1. Objective
Add a CLI/config characterization harness with baseline artifacts and blocking governance linkage checks.

## 2. In-scope / Out-of-scope
In-scope: characterization baselines, CLI/config characterization tests, parity linkage updates for CLI/CFG rows, governance `--check characterization`, and docs updates.
Out-of-scope: runtime behavior changes in `src/jfin`, imaging golden harness, route flips to `v1`, and route-fence status promotion without explicit coverage.

## 3. Public interfaces affected
- `tests/characterization/baselines/cli_contract_baseline.json`
- `tests/characterization/baselines/config_contract_baseline.json`
- `python project/scripts/verify_governance.py --check characterization`
- `project/parity-matrix.md` (`CLI-*` / `CFG-*` row ownership links)

## 4. Acceptance criteria
- All 13 `CLI-*` / `CFG-*` behavior IDs are represented in baseline artifacts and covered by characterization tests.
- Parity matrix CLI/config rows point to resolvable baseline anchors and test node IDs.
- `python project/scripts/verify_governance.py --check characterization` passes on valid linkage and fails on drift.
- Existing parity check remains the sole parity/route schema validator.

## 5. Verification commands (<10 min)
- `$env:PYTHONPATH='src'; python -m pytest -q tests/characterization -k harness`
- `PYTHONPATH=src python -m pytest -q tests/characterization/cli_contract/test_cli_contract_characterization.py`
- `PYTHONPATH=src python -m pytest -q tests/characterization/config_contract/test_config_contract_characterization.py`
- `PYTHONPATH=src python -m pytest -q tests/test_characterization_checks.py`
- `python project/scripts/verify_governance.py --check characterization`
- `python project/scripts/verify_governance.py --check parity`
- `python project/scripts/verify_governance.py --check all`

## 6. Rollback step
Revert WI-004 commits in reverse order with `git revert`: docs/tests, governance wiring, then baseline/parity artifacts.

## 7. Behavior-preservation statement
Preserved. WI-004 adds characterization and governance enforcement only; no runtime behavior changes in `src/jfin`.
